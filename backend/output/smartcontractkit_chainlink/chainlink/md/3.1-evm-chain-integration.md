-----------------------

Chainlink nodes can connect to multiple EVM chains simultaneously, with each chain having multiple RPC endpoints for redundancy.

### Configuration Structure

EVM configuration follows this hierarchical structure:

Sources:

* [docs/CONFIG.md12-106]()
* [core/config/docs/chains-evm.toml1-107]()

### Key Configuration Parameters

Here are the essential configuration options for EVM chains:

| Parameter | Description | Default |
| --- | --- | --- |
| `ChainID` | Required EVM chain identifier | - |
| `Enabled` | Enables this chain | `true` |
| `FinalityDepth` | Blocks needed for transaction finality | Chain-specific |
| `FinalityTagEnabled` | Use chain's finality flag if available | Chain-specific |
| `MinIncomingConfirmations` | Confirmations required for logs | `3` |
| `LogPollInterval` | How often to poll for logs | `15s` |
| `NoNewHeadsThreshold` | Time before considering RPC out of sync | `3m` |

Each chain can have multiple nodes configured:

Sources:

* [docs/CONFIG.md12-106]()
* [core/services/chainlink/testdata/config-multi-chain.toml108-153]()
* [core/services/chainlink/testdata/config-multi-chain-effective.toml317-430]()

Node Pool Management
--------------------

The node pool manages connections to multiple RPC endpoints for each chain, providing failover capability and ensuring high availability.

Sources:

* [core/services/chainlink/testdata/config-multi-chain-effective.toml395-406]()
* [core/services/chainlink/testdata/config-full.toml427-456]()

### Node Selection Strategies

The node pool supports different node selection strategies:

| Strategy | Description |
| --- | --- |
| `HighestHead` | Selects the node reporting the highest block height |
| `RoundRobin` | Rotates through available nodes |
| `PriorityLevel` | Selects based on configured priority |

### Node Health Monitoring

The node pool continuously monitors node health using several indicators:

* **Sync Status**: Ensures the node is in sync with the network
* **Response Time**: Measures RPC request latency
* **Error Rates**: Tracks failed requests
* **Head Currency**: Checks if the node is receiving new blocks
* **Chain ID Verification**: Confirms the node is on the correct chain

Sources:

* [core/services/chainlink/testdata/config-full.toml427-456]()
* [core/web/resolver/testdata/config-full.toml427-456]()

Head Tracking
-------------

The head tracker component subscribes to new blocks and maintains the chain head state.

Sources:

* [core/services/chainlink/testdata/config-multi-chain-effective.toml387-393]()
* [core/services/chainlink/testdata/config-full.toml413-421]()

### Head Tracker Configuration

Key head tracker settings include:

| Parameter | Description | Default |
| --- | --- | --- |
| `HistoryDepth` | Number of past blocks to keep | `100` |
| `MaxBufferSize` | Max blocks to buffer | `3` |
| `SamplingInterval` | Frequency of head sampling | `1s` |
| `FinalityTagBypass` | Whether to honor finality tags | Chain-specific |

The head tracker is crucial for:

* Detecting chain reorganizations
* Providing block height awareness to all node components
* Triggering time-based job executions
* Managing transaction confirmations

Sources:

* [core/services/chainlink/testdata/config-multi-chain-effective.toml387-393]()
* [core/services/ocrcommon/block\_translator.go1-30]()

Transaction Management
----------------------

The transaction manager handles the lifecycle of all blockchain transactions, from creation through confirmation.

Sources:

* [core/services/chainlink/testdata/config-multi-chain-effective.toml341-348]()
* [core/services/chainlink/testdata/config-full.toml358-365]()
* [core/chains/evm/txmgr/test\_helpers.go1-50]()

### Gas Estimation

Chainlink supports several gas pricing strategies:

| Strategy | Description |
| --- | --- |
| `BlockHistory` | Estimates based on recent blocks |
| `FixedPrice` | Uses a fixed gas price |
| `SuggestedPrice` | Uses node's suggestion |
| `L2SuggestedPrice` | Optimized for L2 chains |

Each strategy has configurable parameters for controlling transaction costs and confirmation times.

### Transaction Configuration

Key transaction settings include:

| Parameter | Description | Default |
| --- | --- | --- |
| `ForwardersEnabled` | Use forwarder contracts | `false` |
| `MaxInFlight` | Max concurrent transactions | `16` |
| `MaxQueued` | Max queued transactions | `250` |
| `ReaperInterval` | Cleanup interval | `1h` |
| `ResendAfterThreshold` | Rebroadcast time | `1m` |

Sources:

* [core/services/chainlink/testdata/config-multi-chain-effective.toml341-354]()
* [core/config/docs/chains-evm.toml108-132]()

Contract Integration
--------------------

EVM chain integration includes comprehensive contract interaction capabilities through Go wrappers, contract tracking, and automated compilation systems.

### Contract Wrappers and Deployment

Chainlink uses generated Go wrappers for type-safe contract interactions. The system supports deploying and interacting with various contract types:

### OCR Contract Tracking

The `OCRContractTracker` provides real-time monitoring of OCR contract events and configuration changes:

### Contract Event Handling

The system tracks two primary OCR contract events:

| Event Type | Handler | Purpose |
| --- | --- | --- |
| `ConfigSet` | `ParseConfigSet()` | Monitors OCR configuration changes |
| `RoundRequested` | `ParseRoundRequested()` | Tracks new round requests |

Event processing includes:

* **Duplicate Detection**: Uses `WasAlreadyConsumed()` to prevent reprocessing
* **Transaction Safety**: Database operations are wrapped in transactions
* **Block Ordering**: `IsLaterThan()` ensures events are processed in chronological order
* **Mailbox Buffering**: Configuration changes are buffered through `mailbox.Mailbox`

### Block Translation for Chain Types

Different EVM chains require specific block number translation logic:

Sources:

* [integration-tests/contracts/ethereum\_contracts.go199-261]()
* [integration-tests/contracts/contract\_models.go1-450]()
* [core/services/ocr/contract\_tracker.go50-96]()
* [core/services/ocrcommon/block\_translator.go20-30]()
* [contracts/scripts/native\_solc\_compile\_all1-18]()

Log Handling
------------

The log broadcaster system manages subscriptions to blockchain events (logs) and delivers them to interested services.

### Log Broadcaster Configuration

| Parameter | Description | Default |
| --- | --- | --- |
| `LogBackfillBatchSize` | Batch size for backfilling | `1000` |
| `LogPollInterval` | Polling frequency | `15s` |
| `BlockBackfillDepth` | How far back to look for logs | `10` |
| `LogKeepBlocksDepth` | How many blocks of logs to keep | `100000` |

Sources:

* [core/services/chainlink/testdata/config-multi-chain-effective.toml324-330]()
* [core/config/docs/chains-evm.toml48-62]()

Chain-Specific Adaptations
--------------------------

Chainlink supports various EVM-compatible chains, each with specific adaptations:

Sources:

* [core/config/docs/chains-evm.toml16-18]()
* [core/services/chainlink/config\_test.go580]()
* [core/services/ocrcommon/block\_translator.go22-30]()

### Chain Type Detection and Block Translation

Chainlink automatically detects the chain type based on the Chain ID but can be forced to use a specific type with the `ChainType` configuration parameter. The `BlockTranslator` system handles chain-specific block number translation logic:

#### Block Translation Logic by Chain Type

Supported chain types with specific handling:

* **`arbitrum`**: Uses `ArbitrumBlockTranslator` for L2-specific block translation
* **Standard EVM**: All other chains use `l1BlockTranslator`
  + `celo`, `gnosis`, `kroma`, `metis`, `optimismBedrock`
  + `sei`, `scroll`, `wemix`, `xlayer`, `zkevm`, `zksync`
  + `zircuit`, `rootstock`, `astar`, `hedera`

Each chain type affects:

* **Block Number Translation**: How L1 vs L2 block numbers are handled
* **Query Range Generation**: `NumberToQueryRange()` behavior for log filtering
* **Event Processing**: Chain-specific event handling optimizations

Sources:

* [core/services/ocrcommon/block\_translator.go20-30]()
* [core/config/docs/chains-evm.toml16-18]()
* [core/services/chainlink/config\_test.go580]()

EVM Configuration Examples
--------------------------

### Basic Single Chain Configuration

### Multiple Chain Configuration

### High Availability Configuration

Sources:

* [core/services/chainlink/testdata/config-multi-chain-effective.toml317-649]()
* [docs/CONFIG.md12-19]()

Conclusion
----------

The EVM Chain Integration system provides a robust framework for connecting Chainlink nodes to multiple EVM-compatible blockchains with:

1. **High Availability** through node pool management and failover capabilities
2. **Chain-Specific Optimizations** for diverse EVM networks
3. **Flexible Configuration** to adapt to various deployment scenarios
4. **Transaction Management** with reliable delivery and confirmation
5. **Log Monitoring** to efficiently process blockchain events

These capabilities form the foundation for Chainlink's core services like OCR, VRF, and Automation.

For more information about specific protocol integrations that build on this EVM foundation, refer to:

* [OCR2 (Off-Chain Reporting)]())
* [VRF (Verifiable Random Function)]())
* [Smart Contracts]()